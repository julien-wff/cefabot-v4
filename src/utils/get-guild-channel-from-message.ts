import { Message } from 'discord.js';
import { ChannelType } from '../models/Channel';
import { BotInstance } from '../bot/botTypes';

export default async function getGuildChannelFromMessage(message: Message, bot: BotInstance, channelName: ChannelType) {

    //TODO

}
